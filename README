## README ##

Install
```
git clone git@bitbucket.org:charmpitz/configs.git
cd configs

grep -q "source $PWD/bash_boostrap.sh" ~/.bashrc || echo "source $PWD/bash_boostrap.sh" >> ~/.bashrc

source ~/.bashrc
```

Replace current value
```
sed -i "s|^source.*bash_bootstrap\.sh$|source $PWD\/bash_boostrap.sh|g" ~/.bashrc
```